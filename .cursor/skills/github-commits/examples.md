# Client commit examples

**feat**
```
feat(board): add task deadline chip on the canvas card
```

**fix**
```
fix(auth): clear Hive tokens when refresh fails
```

**l10n**
```
docs(l10n): add vi/en strings for invite revoke
```

**p2p**
```
fix(p2p): ignore duplicate opId after catch-up replay
```
